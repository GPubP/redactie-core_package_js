import Modules from '../modules/modules.class';
import Routes from '../routes/routes.class';
import ActionBar from '../action-bar/action-bar.class';

export default {
	modules: new Modules(),
	routes: new Routes(),
	actionBar: new ActionBar(),
};
